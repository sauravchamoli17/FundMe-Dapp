{
  "name": "hardhat-project",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.2",
    "hardhat": "^2.15.0"
  },
  "scripts": {
    "deploy": "npx hardhat compile && npx hardhat run scripts/deploy.js --network sepolia",
    "verify": "npx hardhat verify $ADDY --network sepolia"
  },
  "dependencies": {
    "@chainlink/contracts": "^0.6.1",
    "dotenv": "^16.1.4"
  }
}
